{
  "name": "@payload-exchange/buyer-agent",
  "version": "0.0.3",
  "type": "module",
  "bin": {
    "px-buyer": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "files": ["dist"],
  "scripts": {
    "start": "bun run src/index.ts",
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@payload-exchange/buyer-sdk": "workspace:*",
    "@payload-exchange/protocol": "workspace:*",
    "commander": "^13.0.0"
  },
  "devDependencies": {
    "@payload-exchange/tsconfig": "workspace:*",
    "@types/bun": "^1.3.11",
    "typescript": "^5.7.0"
  }
}
